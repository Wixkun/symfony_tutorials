<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Créer un Nouvel Utilisateur{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
        <style>
            @layer utilities {
                .main-color {
                    @apply text-red-600;
                }
            }
        </style>
        {% endblock %}
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        
        <section class="container mx-auto px-6 py-16">
            
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Créer un Nouvel Utilisateur</h1>
                <p class="text-gray-600 mt-2">Ajoutez un nouvel utilisateur au système.</p>
            </div>

            
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                {{ form_start(form) }}
                    <div>
                        {{ form_label(form.email, "Email", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.email, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.email) }}
                    </div>
                    <div>
                        {{ form_label(form.username, "Nom d'utilisateur", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.username, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.username) }}
                    </div>
                    <div>
                        {{ form_label(form.name, "Nom", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.name, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.name) }}
                    </div>

                    <div>
                        {{ form_label(form.firstName, "Prénom", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.firstName, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.firstName) }}
                    </div>

                    <div>
                        {{ form_label(form.roles, "Rôles", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.roles, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.roles) }}
                    </div>

                    <div>
                        <button type="submit" class="w-full bg-green-500 text-white px-4 py-3 rounded-lg hover:bg-green-400 transition">
                            Créer
                        </button>
                    </div>
                {{ form_end(form) }}
            </div>

            
            <div class="mt-6 text-center">
                <a href="{{ path('app_admin_user_index') }}" class="text-gray-700 hover:text-red-600 underline">
                    ← Retour à la liste
                </a>
            </div>
        </section>

        
        <footer class="bg-gray-800 text-white py-6">
            <div class="container mx-auto text-center">
                <p class="text-sm">© {{ "now"|date("Y") }} Tutorials. Tous droits réservés.</p>
                <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
            </div>
        </footer>
    </body>
</html>
