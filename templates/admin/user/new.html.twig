<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Créer un Nouvel Utilisateur{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
        <style>
            @layer utilities {
                .main-color {
                    @apply text-red-600;
                }
            }
        </style>
        {% endblock %}
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        
        <section class="container mx-auto px-6 py-16">
            
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Créer un Nouvel Utilisateur</h1>
                <p class="text-gray-600 mt-2">Ajoutez un nouvel utilisateur au système.</p>
            </div>

            
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                <form method="post" class="space-y-6">
                    
                    <div>
                        <label for="email" class="block text-gray-700 font-medium mb-2">Email</label>
                        <input type="email" id="email" name="email" placeholder="Entrez l'email de l'utilisateur" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    
                    <div>
                        <label for="name" class="block text-gray-700 font-medium mb-2">Nom</label>
                        <input type="text" id="name" name="name" placeholder="Entrez le nom de l'utilisateur" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    
                    <div>
                        <label for="firstName" class="block text-gray-700 font-medium mb-2">Prénom</label>
                        <input type="text" id="firstName" name="firstName" placeholder="Entrez le prénom de l'utilisateur" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    
                    <div>
                        <label for="roles" class="block text-gray-700 font-medium mb-2">Rôles</label>
                        <input type="text" id="roles" name="roles" placeholder="Entrez les rôles (ex: ROLE_USER)" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    
                    <div>
                        <button type="submit" class="w-full bg-green-500 text-white px-4 py-3 rounded-lg hover:bg-green-400 transition">
                            Créer
                        </button>
                    </div>
                </form>
            </div>

            
            <div class="mt-6 text-center">
                <a href="{{ path('app_admin_user_index') }}" class="text-gray-700 hover:text-red-600 underline">
                    ← Retour à la liste
                </a>
            </div>
        </section>

        
        <footer class="bg-gray-800 text-white py-6">
            <div class="container mx-auto text-center">
                <p class="text-sm">© {{ "now"|date("Y") }} Tutorials. Tous droits réservés.</p>
                <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
            </div>
        </footer>
    </body>
</html>
