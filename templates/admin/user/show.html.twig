<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Détails de l'Utilisateur{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        <section class="container mx-auto px-6 py-16">
            <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">Détails de l'Utilisateur</h1>
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                <table class="table-auto min-w-full text-left text-gray-700">
                    <tbody>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">ID</th>
                            <td class="px-6 py-3">{{ user.id }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Email</th>
                            <td class="px-6 py-3">{{ user.email }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Nom d'utilisateur</th>
                            <td class="px-6 py-3">{{ user.username }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Nom</th>
                            <td class="px-6 py-3">{{ user.name }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Prénom</th>
                            <td class="px-6 py-3">{{ user.firstName }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Rôles</th>
                            <td class="px-6 py-3">{{ user.roles ? user.roles|json_encode : '' }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="flex justify-between mt-8 max-w-2xl mx-auto">
                <a href="{{ path('app_admin_user_index') }}" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-500 transition">
                    ← Retour à la liste
                </a>
                <a href="{{ path('app_admin_user_edit', {'id': user.id}) }}" class="bg-yellow-600 text-white px-4 py-2 rounded-lg hover:bg-yellow-500 transition ml-4">
                    Modifier
                </a>
                <form method="post" action="{{ path('app_admin_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition">
                        Supprimer
                    </button>
                </form>
            </div>
            </div>
        </section>
    </body>
</html>
