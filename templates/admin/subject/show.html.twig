<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Détails de la Matière{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
        <style>
            @layer utilities {
                .main-color {
                    @apply text-red-600;
                }
            }
        </style>
        {% endblock %}
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        
        <section class="container mx-auto px-6 py-16">
            
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Détails de la Matière</h1>
                <p class="text-gray-600 mt-2">Découvrez les informations détaillées sur cette matière.</p>
            </div>

            
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                <table class="min-w-full text-gray-700">
                    <tbody>
                        <tr>
                            <th class="text-left py-3 px-6 border-b bg-gray-50">ID</th>
                            <td class="py-3 px-6 border-b">{{ subject.id }}</td>
                        </tr>
                        <tr>
                            <th class="text-left py-3 px-6 border-b bg-gray-50">Nom</th>
                            <td class="py-3 px-6 border-b">{{ subject.name }}</td>
                        </tr>
                        <tr>
                            <th class="text-left py-3 px-6 border-b bg-gray-50">Description</th>
                            <td class="py-3 px-6 border-b">{{ subject.description }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>

            
            <div class="flex justify-between mt-8 max-w-2xl mx-auto">
                <a href="{{ path('app_admin_subject_index') }}" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-500 transition">
                    ← Retour à la liste
                </a>
                <a href="{{ path('app_admin_subject_edit', {'id': subject.id}) }}" class="bg-yellow-600 text-white px-4 py-2 rounded-lg hover:bg-yellow-500 transition ml-4">
                    Modifier
                </a>
                <form method="post" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette matière ?');">
                    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition">
                        Supprimer
                    </button>
                </form>
            </div>
        </section>

        
        <footer class="bg-gray-800 text-white py-6">
            <div class="container mx-auto text-center">
                <p class="text-sm">© {{ "now"|date("Y") }} Tutoriels. Tous droits réservés.</p>
                <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
            </div>
        </footer>
    </body>
</html>
