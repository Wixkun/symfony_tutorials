<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Modifier un Utilisateur{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        <!-- Section principale -->
        <section class="container mx-auto px-6 py-16">
            <!-- Titre -->
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Modifier l'Utilisateur</h1>
                <p class="text-gray-600 mt-2">Mettez à jour les informations de l'utilisateur ci-dessous.</p>
            </div>

            <!-- Formulaire -->
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                <form method="post" class="space-y-6">
                    <!-- Nom -->
                    <div>
                        <label for="name" class="block text-gray-700 font-medium mb-2">Nom</label>
                        <input type="text" id="name" name="name" value="{{ user.name }}" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    <!-- Prénom -->
                    <div>
                        <label for="firstName" class="block text-gray-700 font-medium mb-2">Prénom</label>
                        <input type="text" id="firstName" name="firstName" value="{{ user.firstName }}" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    <!-- Email -->
                    <div>
                        <label for="email" class="block text-gray-700 font-medium mb-2">Email</label>
                        <input type="email" id="email" name="email" value="{{ user.email }}" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    <!-- Nom d'utilisateur -->
                    <div>
                        <label for="username" class="block text-gray-700 font-medium mb-2">Nom d'utilisateur</label>
                        <input type="text" id="username" name="username" value="{{ user.username }}" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    <!-- Rôles -->
                    <div>
                        <label for="roles" class="block text-gray-700 font-medium mb-2">Rôles</label>
                        <input type="text" id="roles" name="roles" value="{{ user.roles|join(', ') }}" class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200">
                    </div>

                    <!-- Bouton Mettre à jour -->
                    <div>
                        <button type="submit" class="w-full bg-orange-500 text-white px-4 py-3 rounded-lg hover:bg-orange-400 transition">
                            Mettre à jour
                        </button>
                    </div>
                </form>
            </div>

            <!-- Boutons secondaires -->
            <div class="flex justify-between mt-8 max-w-2xl mx-auto">
                <a href="{{ path('app_admin_user_index') }}" class="text-gray-700 hover:text-red-600 underline">
                    ← Retour à la liste
                </a>
                <form method="post" action="{{ path('app_admin_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
                    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition">
                        Supprimer
                    </button>
                </form>
            </div>
        </section>

        <!-- Footer -->
        <footer class="bg-gray-800 text-white py-6">
            <div class="container mx-auto text-center">
                <p class="text-sm">© {{ "now"|date("Y") }} Tutoriels. Tous droits réservés.</p>
                <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
            </div>
        </footer>
    </body>
</html>
