<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Modifier une Matière{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        
        <section class="container mx-auto px-6 py-16">
            
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Modifier une Matière</h1>
                <p class="text-gray-600 mt-2">Mettez à jour les informations de la matière ci-dessous.</p>
            </div>

            
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                {{ form_start(form) }}
                    <div>
                        {{ form_label(form.name, "Nom", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.name, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.name) }}
                    </div>

                    <div>
                        {{ form_label(form.description, "Description", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.description, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                        {{ form_errors(form.description) }}
                    </div>

                    <div>
                        <button type="submit" class="w-full bg-orange-500 text-white px-4 py-3 rounded-lg hover:bg-orange-400 transition">
                            Mettre à jour
                        </button>
                    </div>
                {{ form_end(form) }}
            </div>

            <div class="flex justify-between mt-8 max-w-2xl mx-auto">
                <a href="{{ path('app_admin_subject_index') }}" class="text-gray-700 hover:text-red-600 underline">
                    ← Retour à la liste
                </a>
                
                
                <form method="post" action="{{ path('app_admin_subject_delete', {'id': subject.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette matière ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subject.id) }}">
                    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition">
                        Supprimer
                    </button>
                </form>
            </div>
        </section>
    </body>
</html>
