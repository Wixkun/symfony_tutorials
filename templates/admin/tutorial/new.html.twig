<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Créer un Nouveau Tutoriel{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
        <style>
            @layer utilities {
                .main-color {
                    @apply text-red-600;
                }
            }
        </style>
        {% endblock %}
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        
        <section class="container mx-auto px-6 py-16">
            
            
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Créer un Nouveau Tutoriel</h1>
                <p class="text-gray-600 mt-2">Ajoutez un nouveau tutoriel au système.</p>
            </div>

            
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                {{ form_start(form) }}
                
                
                <div class="mb-4">
                    {{ form_label(form.title, "Titre du Tutoriel", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                    {{ form_widget(form.title, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                    {{ form_errors(form.title) }}
                </div>

                
                <div class="mb-4">
                    {{ form_label(form.description, "Description", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                    {{ form_widget(form.description, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                    {{ form_errors(form.description) }}
                </div>

                
                <div class="mb-4">
                    {{ form_label(form.subject, "Matière", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                    {{ form_widget(form.subject, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                    {{ form_errors(form.subject) }}
                </div>

                
                <div class="mb-4">
                    {{ form_label(form.content, "Contenu", {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                    {{ form_widget(form.content, {'attr': {'class': 'w-full px-4 py-3 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-red-200'}}) }}
                    {{ form_errors(form.content) }}
                </div>

                
                <div>
                    <button type="submit" class="w-full bg-green-500 text-white px-4 py-3 rounded-lg hover:bg-green-400 transition">
                        Créer
                    </button>
                </div>
                
                {{ form_end(form) }}
            </div>

            
            <div class="mt-6 text-center">
                <a href="{{ path('app_admin_tutorial_index') }}" class="text-gray-700 hover:text-red-600 underline">
                    ← Retour à la liste
                </a>
            </div>
        </section>

        
        <footer class="bg-gray-800 text-white py-6">
            <div class="container mx-auto text-center">
                <p class="text-sm">© {{ "now"|date("Y") }} Tutorials. Tous droits réservés.</p>
                <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
            </div>
        </footer>
    </body>
</html>
