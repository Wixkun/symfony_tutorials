<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Détails du Tutoriel{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        <section class="container mx-auto px-6 py-16">
            <h1 class="text-3xl font-bold text-gray-800 mb-6 text-center">Détails du Tutoriel</h1>
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                <table class="table-auto min-w-full text-left text-gray-700">
                    <tbody>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">ID</th>
                            <td class="px-6 py-3">{{ tutorial.id }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Titre</th>
                            <td class="px-6 py-3">{{ tutorial.title }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Description</th>
                            <td class="px-6 py-3">{{ tutorial.description }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Date de Création</th>
                            <td class="px-6 py-3">{{ tutorial.creationDate ? tutorial.creationDate|date('d/m/Y') : '' }}</td>
                        </tr>
                        <tr>
                            <th class="px-6 py-3 bg-gray-100">Contenu</th>
                            <td class="px-6 py-3">{{ tutorial.content }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="flex justify-between mt-8 max-w-2xl mx-auto">
                <a href="{{ path('app_admin_tutorial_index') }}" class="bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-500 transition">
                    ← Retour à la liste
                </a>
                <a href="{{ path('app_admin_tutorial_edit', {'id': tutorial.id}) }}" class="bg-yellow-600 text-white px-4 py-2 rounded-lg hover:bg-yellow-500 transition ml-4">
                    Modifier
                </a>
            </div>
        </section>
    </body>
</html>
