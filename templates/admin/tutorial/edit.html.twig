<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Modifier un Tutoriel{% endblock %}</title>
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="bg-gray-100 font-sans leading-normal tracking-normal">
        
        <section class="container mx-auto px-6 py-16">
            
            <div class="text-center mb-8">
                <h1 class="text-4xl font-extrabold text-gray-800">Modifier le Tutoriel</h1>
                <p class="text-gray-600 mt-2">Mettez à jour les informations du tutoriel ci-dessous.</p>
            </div>

            
            <div class="bg-white p-8 rounded-lg shadow-lg max-w-2xl mx-auto">
                {{ form_start(form, {'attr': {'class': 'space-y-6'}}) }}
                    <div>
                        {{ form_label(form.title, null, {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.title) }}
                        {{ form_errors(form.title) }}
                    </div>

                    <div>
                        {{ form_label(form.description, null, {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.description) }}
                        {{ form_errors(form.description) }}
                    </div>

                    <div>
                        {{ form_label(form.content, null, {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.content) }}
                        {{ form_errors(form.content) }}
                    </div>

                    <div>
                        {{ form_label(form.subject, null, {'label_attr': {'class': 'block text-gray-700 font-medium mb-2'}}) }}
                        {{ form_widget(form.subject) }}
                        {{ form_errors(form.subject) }}
                    </div>

                    <div>
                        <button type="submit" class="w-full bg-orange-500 text-white px-4 py-3 rounded-lg hover:bg-orange-400 transition">
                            Mettre à jour
                        </button>
                    </div>
                {{ form_end(form) }}
            </div>

            
            <div class="flex justify-between mt-8 max-w-2xl mx-auto">
                <a href="{{ path('app_admin_tutorial_index') }}" class="text-gray-700 hover:text-red-600 underline">
                    ← Retour à la liste
                </a>
                <form method="post" action="{{ path('app_admin_tutorial_delete', {'id': tutorial.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce tutoriel ?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tutorial.id) }}">
                    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition">
                        Supprimer
                    </button>
                </form>
            </div>
        </section>

        
        <footer class="bg-gray-800 text-white py-6">
            <div class="container mx-auto text-center">
                <p class="text-sm">© {{ "now"|date("Y") }} Tutorials. Tous droits réservés.</p>
                <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
            </div>
        </footer>
    </body>
</html>
