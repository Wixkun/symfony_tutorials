<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Profil</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 font-sans leading-normal tracking-normal">
    <header class="bg-white shadow">
        <div class="container mx-auto flex justify-between items-center py-4 px-6">
            <a href="/" class="text-2xl font-bold text-red-600">Tutoriels</a>
            <div class="flex items-center space-x-4">
                <a href="{{ path('app_home') }}" class="text-gray-600 hover:text-red-600">Accueil</a>
                <a href="{{ path('app_logout') }}" class="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-500 transition">
                    Déconnexion
                </a>
            </div>
        </div>
    </header>

    <main class="container mx-auto px-6 py-16">
        <h1 class="text-4xl font-bold text-gray-800 text-center mb-12">Mon Profil</h1>
        
        <div class="bg-white p-8 rounded-lg shadow-lg">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Vos informations</h2>
            <p class="text-gray-600"><strong>Nom :</strong> {{ app.user.name }}</p>
            <p class="text-gray-600"><strong>Prénom :</strong> {{ app.user.firstName }}</p>
            <p class="text-gray-600"><strong>Email :</strong> {{ app.user.email }}</p>
            <p class="text-gray-600"><strong>Nom d'utilisateur :</strong> {{ app.user.username }}</p>
            <p class="text-gray-600"><strong>Rôles :</strong> {{ app.user.roles | join(', ') }}</p>

            <div class="mt-6">
                <a href="#" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-500 transition">
                    Modifier mes informations
                </a>
            </div>
        </div>
    </main>

    <footer class="bg-gray-800 text-white mt-12">
        <div class="container mx-auto px-6 py-8 text-center">
            <p class="text-sm">© {{ "now"|date("Y") }} Tutoriels. Tous droits réservés.</p>
            <p class="text-sm mt-2">Fait avec ❤️ pour les apprenants du monde entier.</p>
        </div>
    </footer>
</body>

</html>
