<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Compte Banni</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            height: 100vh;
            overflow: hidden;
        }
    </style>
</head>

<body class="bg-red-100 font-sans leading-normal tracking-normal">
    
    <header class="bg-red-600 text-white py-4">
        <div class="container mx-auto flex justify-between items-center px-6">
            <h1 class="text-xl font-bold">Compte Banni</h1>
            <form method="get" action="{{ path('app_logout') }}">
                <button type="submit" class="bg-white text-red-600 font-medium px-4 py-2 rounded-lg hover:bg-gray-100">
                    Se Déconnecter
                </button>
            </form>
        </div>
    </header>

    
    <div class="flex items-center justify-center h-[calc(100vh-64px)]">
        <div class="bg-white p-8 rounded-lg shadow-lg text-center">
            <h1 class="text-4xl font-bold text-red-600 mb-4">Accès Refusé</h1>
            <p class="text-gray-700">{{ message }}</p>
        </div>
    </div>
</body>

</html>
