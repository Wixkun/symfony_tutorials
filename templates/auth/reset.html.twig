<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réinitialisation du mot de passe</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 font-sans leading-normal tracking-normal">
<div class="min-h-screen flex items-center justify-center">
    <main class="w-full max-w-md mx-auto p-6">
        <!-- Header -->
        <header class="text-center mb-6">
            <a href="/" class="text-2xl font-bold text-red-600">
                Réinitialisation du mot de passe
            </a>
        </header>

        <!-- Flash Messages -->
        <section class="mb-4">
            {% for type, messages in app.flashes %}
                {% for message in messages %}
                    <div class="p-4 mb-4 rounded-lg border {{ type == 'success' ? 'bg-green-100 border-green-400 text-green-700' : 'bg-red-100 border-red-400 text-red-700' }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </section>

        <!-- Reset Password Form -->
        <div class="bg-white p-8 rounded-lg shadow">
            <h1 class="text-xl font-bold text-gray-800 text-center mb-6">Réinitialisez votre mot de passe</h1>
            <p class="text-center text-sm text-gray-600 mb-6">
                Entrez votre nouveau mot de passe ci-dessous.
            </p>
            <div class="bg-orange-100 border border-orange-300 text-orange-700 px-4 py-3 rounded mb-6">
                Votre mot de passe doit contenir au moins 8 caractères, une majuscule, une minuscule et un chiffre.
            </div>
            <form method="POST" action="{{ path('page_reset_password', { 'token': token }) }}" class="space-y-4">
                <!-- Password Field -->
                <div>
                    <input 
                        name="password" 
                        class="w-full px-4 py-3 rounded-lg bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-red-500 focus:bg-white" 
                        id="password" 
                        type="password" 
                        placeholder="Nouveau mot de passe" 
                        required />
                </div>
                <!-- Confirm Password Field -->
                <div>
                    <input 
                        name="repeat_password" 
                        class="w-full px-4 py-3 rounded-lg bg-gray-100 border border-gray-200 placeholder-gray-500 text-sm focus:outline-none focus:border-red-500 focus:bg-white" 
                        id="repeat-password" 
                        type="password" 
                        placeholder="Confirmez votre nouveau mot de passe" 
                        required />
                </div>
                <!-- Submit Button -->
                <div class="mt-6">
                    <button 
                        type="submit" 
                        class="w-full bg-red-600 text-white px-4 py-3 rounded-lg hover:bg-red-500 transition">
                        Changer mon mot de passe
                    </button>
                </div>
            </form>
            <!-- Back to Login -->
            <div class="mt-6 text-center">
                <a href="{{ path('app_login') }}" 
                   class="text-red-600 hover:text-red-700 transition">
                    Vous vous souvenez de votre mot de passe ? Connectez-vous
                </a>
            </div>
        </div>
    </main>
</div>
</body>

</html>
